<div>
    @for (int i = 1; i <= 10; i++)
    {
        <div>
            @for (int j = 1; j <= 10; j++)
            {
                <a href="@Url.Action("GetDividedNumber", new { id = (i * j) })">@(i * j)</a>
            }
        </div>
    }
</div>